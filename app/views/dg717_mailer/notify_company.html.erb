<tr>
  <td align="center" colspan="2" style="padding:40px 0 10px; font-size:1.2em;">
    <p>This is a test e-mail about the meeting room usage. The charge will not incure until we roll the service in January of 2016.</p>
    <% if @mode = 0 %>
      <% if @company.over_usage?  %>
        <%= "Dear #{@company.user.first_name unless @company.user.nil?}, your team at #{@company.name} has used #{@company.excess_usage} hours over the included allowance of #{@company.monthly_allowance} hours. You will be invoiced $25 per hour over the allowance." %>
      <% else %>
        <%=  "Dear #{@company.user.first_name unless @company.user.nil?}, your team at #{@company.name} has almost used the included allowance of #{@company.monthly_allowance} hours. You will be invoiced $25 per hour once you go over the allowance."%>
      <% end %> 
    <% else %> 
      <%=  "Dear #{@company.user.first_name unless @company.user.nil?}, Below are usage by each of your team member in your company #{@company.name}. You will be invoiced $25 per hour once you go over the allowance for the company."%>      
    <% end %>
  </td>
</tr>
<tr>
  <td align="center" colspan="2" style="padding:30px; font-size: 1.2em;">Below are the usage breakdowns per your team member.<br />It includes the currently reserved hours for this month.</td>
</tr>
<tr>
<td align="center" colspan="2" style="padding-bottom:30px;">
  <table style="background-color: rgba(0,0,0,0.1); border-radius:10px; padding:10px;">
  <% @company.users.each do |user| %>
  <tr>
    <td style="padding:10px; font-weight:bold;" align=right>
      <%= user.user_name %>
    </td>
    <td style="padding:10px;">
      <% if user.available_hours > 0 %>
        <%= "#{user.used_hours.to_i} out of #{user.limited_hours.to_i} hours used" %>
      <% else %>
       <span style='color:red;'>
        <%= "#{user.used_hours.to_i} hours used (#{user.excess_usage.to_i} hours over used)" %>
        </span>
      <% end %>
    </td>
  </tr>
  <% end %>
  <tr><td colspan="2" style="font-size:1.2em; border-top:1px solid #999; padding:10px 0 10px; color:#333;" align=center><%= @company.monthly_usage.round %> hours / <%= @company.monthly_allowance %> hours used</td></tr> 
  <% if @company.over_usage? %>
  <tr><td colspan="2" style="font-size:1.2em; padding:10px 0 10px; padding:5px; color:#fff; background-color: rgba(0,0,0,0.5); border-radius:5px;" align=center>
    <%= "#{@company.excess_usage} overused" %><br />
    <%= "#{@company.excess_usage * 25} USD will be invoiced." %>
  </td></tr>

  </table></td></tr>
  <% end %>
